<%= form_for(@preferencial) do |f| %>
  <% if @preferencial.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@preferencial.errors.count, "error") %> prohibited this preferencial from being saved:</h2>

      <ul>
      <% @preferencial.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.select("cliente_id" ,Cliente.all.collect{|l| [l.nombre]}, {include_blank:'select Cliente'})%>
  </div>
  <div class="field">


  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
